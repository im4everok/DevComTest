@model BLL.Models.PersonModel;
@using X.PagedList.Mvc.Core;
@{
    ViewData["Title"] = "Delete pet owner";
}

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4 class="btn">Pet owner</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
    </dl>
    @*@using (Html.BeginForm("Delete", "Person", FormMethod.Post))
    {
        @Html.HiddenFor(x => x.Id);
        @Html.ActionLink("Delete", "Delete", "Person", new { id = Model.Id}, new { @class ="btn btn-danger"});
        @Html.ActionLink("Back to all users", "AllUsers", "Person", null, new { @class = "btn btn-primary"})
    }*@
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" />
        @Html.ActionLink("Back to all users", "AllUsers", "Person", null, new { @class = "btn btn-primary"})
    </form>
</div>
